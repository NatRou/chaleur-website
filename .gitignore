# macOS
.DS_Store

# Dependencies
node_modules/

# Environment / secrets
.env
.env.*

# Claude Code local config (preview launch config, etc.)
.claude/

# Claude Design handoff bundle (source/spec only — never ship the raw export)
design-bundle/

# Local backup of the original (black-matte) screenshot webp — not shipped
.image-originals/

# FUSE editor leftovers — never commit
.fuse_hidden*
